{
  "name": "@bsi/sports-plugins",
  "version": "0.1.0",
  "private": true,
  "description": "Curated sports plugins for Claude Code, Codex, and AI coding assistants — college baseball sabermetrics, live scores, analytics, and fantasy sports.",
  "author": "Austin Humphrey <Austin@BlazeSportsIntel.com>",
  "license": "SEE LICENSE IN LICENSE",
  "repository": {
    "type": "git",
    "url": "https://github.com/ahump20/Sports-Plugins.git"
  },
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate": "node scripts/validate-plugins.js",
    "check": "npm run typecheck && npm run lint && npm run test && npm run validate"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "eslint": "^9.25.1",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.31.0",
    "vitest": "^3.1.2"
  }
}
